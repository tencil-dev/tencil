/**
 * @tencil/schema-ui
 *
 * UI/UX domain schema for Tencil.
 * Provides TypeScript types for UI design elements that can be used in `.tencil` files.
 *
 * Includes:
 * - TencilFrame (flex containers)
 * - TencilText (typography)
 * - TencilRectangle (basic shapes)
 * - TencilEllipse (circles/ovals)
 * - TencilPath (vector paths)
 */

export type {
  FlexDirection,
  AlignmentMode,
  TencilFrame,
  TencilText,
  TencilRectangle,
  TencilEllipse,
  TencilPath,
  TencilUINode,
} from "./types.js";
